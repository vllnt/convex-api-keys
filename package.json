{
  "name": "@vllnt/convex-api-keys",
  "version": "0.1.0",
  "description": "Convex component for secure API key management — create, validate, revoke, rotate, rate-limit, track usage",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/client/index.d.ts",
      "default": "./dist/client/index.js"
    },
    "./types": {
      "types": "./dist/client/types.d.ts",
      "default": "./dist/client/types.js"
    },
    "./test": "./src/test.ts",
    "./_generated/component.js": {
      "types": "./dist/component/_generated/component.d.ts"
    },
    "./convex.config": {
      "types": "./dist/component/convex.config.d.ts",
      "default": "./dist/component/convex.config.js"
    },
    "./convex.config.js": {
      "types": "./dist/component/convex.config.d.ts",
      "default": "./dist/component/convex.config.js"
    }
  },
  "types": "./dist/client/index.d.ts",
  "module": "./dist/client/index.js",
  "scripts": {
    "build": "tsc --project ./tsconfig.build.json",
    "build:codegen": "npx convex codegen --component-dir ./src/component && npm run build",
    "build:clean": "rm -rf dist *.tsbuildinfo && npm run build:codegen",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "preversion": "npm ci && npm run build:clean && npm run test && npm run typecheck",
    "alpha": "npm version prerelease --preid alpha && npm publish --tag alpha && git push --follow-tags",
    "release": "npm version patch && npm publish && git push --follow-tags"
  },
  "dependencies": {
    "@convex-dev/aggregate": "^0.2.0",
    "@convex-dev/crons": "^0.2.0",
    "@convex-dev/rate-limiter": "^0.3.0",
    "@convex-dev/sharded-counter": "^0.2.0",
    "@vllnt/logger": "^0.1.2"
  },
  "peerDependencies": {
    "convex": "^1.33.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^4.0.0",
    "@vllnt/eslint-config": "^1.0.0",
    "@vllnt/typescript": "^1.0.0",
    "convex": "^1.33.0",
    "convex-test": "^0.0.36",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vllnt/convex-api-keys"
  },
  "keywords": [
    "convex",
    "api-keys",
    "authentication",
    "rate-limiting",
    "multi-tenant",
    "convex-component"
  ]
}
